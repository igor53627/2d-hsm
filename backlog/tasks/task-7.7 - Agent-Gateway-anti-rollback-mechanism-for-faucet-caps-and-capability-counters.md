---
id: TASK-7.7
title: Agent Gateway anti-rollback mechanism for faucet caps and capability counters
status: In Progress
assignee: []
created_date: '2026-06-07 00:00'
updated_date: '2026-06-08 19:37'
labels:
  - agent-gateway
  - tee
  - anti-rollback
  - security
dependencies:
  - TASK-7.1
  - TASK-7.2
  - TASK-7.4
references:
  - backlog/docs/agent-gateway-anti-rollback.md
  - backlog/docs/agent-gateway-secp256k1-signer-design.md
priority: high
ordinal: 7070
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Define the production anti-rollback mechanism for Agent Gateway sealed replay counters and faucet spend caps. Standard sealed storage gives confidentiality and integrity but cannot by itself stop a compromised host from rolling sealed state back, so production fund custody needs an external anti-rollback authority or an explicit funding block.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The design selects one anti-rollback mechanism for production fund custody: external append-only ledger, remote monotonic counter, operator-signed boot authorization with high-water marks (which must itself be replay-resistant — bound to a platform/hardware monotonic counter or a remote challenge-response — so a host cannot replay a stale sealed state together with its matching stale authorization), or another reviewed equivalent.
- [x] #2 The mechanism covers administrative capability replay counters and faucet cumulative spend counters.
- [x] #3 Restore and failover procedures seed counter high-water marks from authenticated material and never reset counters to zero from a stale backup.
- [x] #4 Active-active clones of one faucet key remain prohibited unless the mechanism provides a global spend/capability ledger shared by every live clone.
- [x] #5 If no production anti-rollback mechanism is available, the task defines the code/config/runbook gate that blocks material production fund custody for Agent Gateway faucet and transfer wallets.
- [x] #6 Roborev matrix/compact evidence is recorded before merge.
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Design delivered in backlog/docs/agent-gateway-anti-rollback.md (design here; the anti-rollback anchor IMPLEMENTATION is TASK-7.7's own — slices tracked below — while TASK-7.6 owns the secp256k1 signer backend it binds onto). Platform: SEV-SNP has NO per-enclave hardware monotonic counter -> external anchor required. Selected Option A = remote monotonic counter + epoch-lease: freshness_epoch in the pq-agent-keystore-v1 ENCRYPTED BODY (format extension per 7.2 AC#16; NOT the pq-seal-v1 AAD); mutual-authenticated anchor handshake (agent-domain SNP report_data + Ed25519-signed anchor response vs pinned anchor_root); on epoch != anchor-current never trust the stale blob's own marks (anti-rollback): ADOPT the anchor's authoritative counter/spend marks when they fully resolve the gap (bounded crash-reconcile), else FAIL CLOSED (epoch > anchor-current anchor-rollback, a structural key/config-mutation gap the anchor never held -> restore, or anchor unavailable); per-dispense bump+seal-before-emit. Default lease=1; a NAIVE lease=N is UNBOUNDED, so a safe lease=N requires anchor-visible per-spend consumed-cursor ack before emit, and admin/recovery/config advances are always lease=1; when the anchor is unavailable ALL fund custody fails closed (no offline window). Crash reconcile: the anchor records authoritative post-op marks (epoch + counter/spend high-water, NOT key material) and the enclave ADOPTS them for counter/spend gaps (never guesses non-emission); a structural key/config-mutation gap the anchor can't supply fails closed -> restore. Covers cap counters + faucet cumulative/lifetime spend + strict recovery counter (AC#2); boot/restore seed from authenticated marks never-zero (AC#3); active-active operator-procedural under A, enforced only by Option B global ledger (AC#4); AC#5 gate = 2-layer fail-closed (Nix assertion with explicit opt-out term + derived enabled; Rust block on rollback-sensitive commands, SIGN_TRANSFER excluded, EXPORT/RESTORE included) + hard-block-default + measured/sealed audited opt-out. Anchor under separation-of-duties + anti-rollback-durable; liveness-DoS is an accepted availability residual.

Roborev evidence (AC#6): 3x3 matrix on 524c8d8 -> 3 HIGH + 3 MED (job 7704); /code-review skill -> 40 candidates -> 15 findings; 9 PR bot comments resolved/replied (CodeRabbit confirmed); post-merge compact -> 3 more (anchor-unavailable, reconcile non-emission proof, stale notes) resolved.

The ACs/DoD above are the **design** acceptance (complete); this task stays In Progress to track the implementation slices below. The anchor module is TASK-7.7's own mechanism, built under the shared `agent-gateway` feature.

Implementation progress: **slice 1 — anchor-response verify + boot reconcile (verify-only)** landed in `impl/rust/enclave-protocol/src/agent_anchor.rs`. Pure, unit-tested with a mock anchor key (14 tests): strict **structural** validation of the decoded **v1-PROVISIONAL** freshness response map (keys 1..=7 always signed + optional 8/9 when chain-bound + key 13 Ed25519 sig) over the value-map API `verify_anchor_response`; the strict *canonical wire* decode for host bytes is now **landed** (sub-slice 1, PR-A) and reachable via `verify_anchor_response_bytes`, which the boot-wiring slice calls. `verify_strict` vs sealed `anchor_root`, scope `(twod_chain_id, environment_identifier)` + fresh-nonce echo binding, the §3 counter/spend-bounded `reconcile` (Fresh/AdoptForward/FailClosed{AnchorBehind,StructuralGap,Inconsistent}), and `anchor_handshake_report_data` (the SNP report_data the next slice's quote must commit to). Wire schema documented in design doc §8. **Status (post PR-B):** `structural_version` (key 5) is **FROZEN v1** and the `marks_digest` (key 6) **enclave encoder is FROZEN v1**; only key 6's cross-component contract (anchor data model) + the marks delivery/adoption path remain pinned/deferred. Nothing is wired at boot yet. Hybrid "Variant C" = §3 Option-A verify mechanism + optional chain-block binding (keys 8/9) so a chain-bridge anchor can back it without a wire change.

**Blocking implementation sub-slices (ordered; each gates the next; live GENERATE_KEYS un-gate via TASK-18 is LAST):**
1. **Strict canonical CBOR decoder** (shared with capability/dispatch) — **LANDED (PR-A, `src/agent_cbor.rs`)**: `strict_decode_map` rejects non-shortest ints, indefinite lengths, dup/out-of-order keys (every nesting level), reserved/tag/float (bool-only major 7), depth/size caps, and trailing bytes; wired into `decode_envelope` (envelope + nested cap/payload submaps); `verify_anchor_response_bytes` is the anchor entrypoint (dead-code until boot). Also consolidated the duplicated int-key accessors. ~22 unit + parity tests.
2. **Nonce plumbing** — **LANDED (`src/agent_challenge.rs`)**: enclave freshness-challenge state machine — `issue_challenge` (CSPRNG draw, overwrite-on-reissue) into a volatile process-global; `Challenge::report_data()` computes the report_data the SNP quote will commit to (same draw); `verify_outstanding_response(response, config)` is the **safe** verify primitive — it consumes (take) BEFORE verifying, so single-use is structural (no non-consuming peek); `consume_outstanding_challenge()` retires the no-response/timeout path; volatile-only enforced structurally (non-Serialize); poison-recover uniformly. 11 tests incl. single-use-replay-is-none, verify-consumes-even-on-failure, entropy. DEFERRED to boot-wiring: actual SNP-quote bytes fetch + host relay; `issue_challenge` runs after unseal with `(chain_id,env)` == sealed config (verify binds scope to config); boot uses `Challenge::report_data()` from issue → `verify_outstanding_response` → `consume_outstanding_challenge` only for timeout retirement.
3. **Pin `structural_version`** — **LANDED (PR-B)**: sealed-body `u64` init 1, required (no serde default), forward-only, bumped per committed GENERATE_KEYS (LOCAL-ONLY + INERT — epoch advance deferred) via `checked_add`→SealFailed; CONFIGURE_TREASURY bump is a deferred handler (exhaustive-`match` obligation). `KEYSTORE_FORMAT_VERSION 1→2` (v1 never shipped, no reader). FROZEN in design doc §8.
4. **Pin `marks_payload`/`marks_digest`** — **LANDED (PR-B)**: `KeystoreBody::compute_local_marks_digest` = SHA3-256(MARKS_DOMAIN ‖ canonical-CBOR{1:sorted counter rows, 2:cumulative_native_spend 32B, 3:lifetime_spend 32B, 4:strict_recovery_counter}); scope_class as CBOR uint, env folded, length-prefixed, hand-derived genesis golden. `strict_recovery_counter` field added (init 0, mutators deferred). Enclave encoder FROZEN; **key-6 cross-component contract PINNED-BEFORE-ANCHOR-CO-SIGN** (anchor data model in §8). **5b-2e LANDED (AdoptForward executable)**: the separate `anchor_root`-signed raw-marks channel (0x44) + strict decoder + recompute-before-adopt + seed-forward + re-run. **The seed gate is `SHA3-256(MARKS_DOMAIN ‖ decoded_marks) == state.marks_digest` (byte-exact hash-equality vs the anchor's signed digest) — MANDATORY. NOT the weaker `adopted ≥ local`** (which would let an untrusted host forge large-but-`≥-local` marks to inflate spend/counter ceilings); `adopted ≥ local` is kept ONLY as a defense-in-depth belt computed strictly AFTER the hash gate, never the gate (AC#3).
5. **Boot wiring (READ-SIDE only)** — SNP-quote fetch + host relay + the boot decode via `verify_anchor_response_bytes`. **Ordering guard:** boot `reconcile` MUST NOT gate any decision on `structural_version` until slice 6's atomic epoch+structural advance ships — otherwise a preview-sealed GENERATE_KEYS candidate (structural bumped, anchor epoch unchanged) fails closed `Inconsistent` on next boot. Until then `structural_version` stays inert; partial-init must not expose rollback-sensitive commands.
6. **Per-op epoch bump + seal-before-emit (lease=1)** — advances `freshness_epoch` + the anchor ack **ATOMICALLY with the `structural_version` bump** (this is the write-side that makes `structural_version` load-bearing/non-forgeable). Only after this lands may slice 5's `reconcile` gate on `structural_version`. (The atomicity invariant is owned HERE, not split with slice 5.)
   - **Decomposition (mirrors the read-side build order; each an independently-mergeable Full-Matrix-gated PR):**
     **6-1 (pure verify — DONE, merged PR #70 squash 5d28066):** `verify_commit_ack_bytes` (4th domain `COMMIT_ACK_DOMAIN`, Ed25519 verify_strict vs sealed anchor_root, scope+nonce+epoch+structural+marks_digest+request_id echo) + commit-request codec (`encode/decode_anchor_commit_request`, 0x45 `AgentAnchorCommitRelay` — enclave-initiated, NOT serve-dispatchable) + `test_signed_commit_ack_bytes` single wire source. Pure/deviceless.
     **6-2 (atomic bump — IN PROGRESS):** `KeystoreBody::advance_commit_epoch(bumps_structural)` — `freshness_epoch += 1` ALWAYS + `structural_version += 1` (if structural), as ONE atomic checked unit (both computed BEFORE either assigned → overflow on either aborts both, no partial mutation; `MonotonicOverflow`). + `AgentOpcode::commit_bump_class` → `CommitBumpClass{Structural,EpochOnly,NotCommitted}` exhaustive/wildcard-free, consistency-pinned vs `is_rollback_sensitive`; GENERATE_KEYS+CONFIGURE_TREASURY=Structural (anchor-unreconstructable), faucet/export/restore=EpochOnly (marks-captured/re-presentable), under-classification is the dangerous direction so doubt⇒Structural. Staged (6-4 wires it).
     **6-3 (commit transport seam — IN PROGRESS):** RE-SCOPED — the commit ACK is small (~200 B, fits the 4096 freshness cap), so NO new channel method is needed: the serve-time commit reuses the existing `BootRelayChannel::round_trip`. 6-3 delivers the PURE composition helper `agent_boot_relay::run_anchor_commit<C: BootRelayChannel>(channel, &AnchorCommit, config, deadline) -> Result<(), CommitFailure{Encode,Transport,Ack}>` (encode 0x45 request → `round_trip` → `verify_commit_ack_bytes`; the request AND the expected-ack are both built from the ONE `AnchorCommit` so they can't drift; `Ok(())` = the seal-before-emit GO signal, ANY failure = fail-closed) + `host_anchor_relay` 0x45 forward arm (4096 cap; "commit request" in the malformed-request bucket) + lab-stub `lab_anchor_commit_reply` (signs an ACK echoing the proposed state) + a deviceless conformance test (stub→guest verify) + the `host_anchor_relay` 0x45 forward arm (DONE here). **6-3 only TRANSPORTS + VERIFIES a CALLER-PROVIDED nonce** (`AnchorCommit.nonce`, echo-checked by `verify_commit_ack_bytes` `NonceMismatch`); it does NOT generate the nonce. Scope is derived from the sealed `config` inside `run_anchor_commit` (request scope == verify scope == config, no caller-supplied scope). **Fresh per-op NONCE GENERATION + the serial-vs-multi-slot concurrency invariant + the process-global commit-channel INSTALL + the dispatch call are ALL owned by 6-4** (the live wiring).
     **6-4 (large — SPLIT into 6-4a dispatch core + 6-4b boot install):** **6-4a (DONE-pending-review):** the seal-before-emit dispatch core, channel injected as a process-global so it's fully unit-testable with a mock — `handle_generate_keys` replaces the INERT structural-only bump with `advance_commit_epoch(Structural)` (epoch+structural LIVE atomic); `AgentResponse::GenerateKeys` widened with `request_id`; new `INSTALLED_COMMIT_CHANNEL: Mutex<Option<Box<dyn BootRelayChannel + Send>>>` + `install_commit_channel` (install-once, 6-4b boot caller) + `draw_commit_nonce` (fresh per-op getrandom) + `COMMIT_ROUND_TRIP_BUDGET` + `commit_candidate_to_anchor` (draw nonce → AnchorCommit from candidate → `run_anchor_commit` under the KEYSTORE→COMMIT_CHANNEL lock order); `handle_agent_gateway_frame` GenerateKeys arm order = SEAL (compute, side-effect-free) → COMMIT → swap → emit: ANY commit failure (no channel/transport/forged ack/getrandom) ⇒ `SealFailed`, NO swap/emit. The SEAL is computed BEFORE the commit (matrix codex-sec fix, job 7233): a DETERMINISTIC seal failure — `BlobTooLarge` on a host-influenced over-cap keygen batch, or a `validate()` reject — then fails closed with NO anchor commit (else the anchor durably advances to a state that can NEVER be sealed ⇒ spurious next-boot StructuralGap→restore for a recoverable condition); the COMMIT still strictly precedes swap/emit, so anchor ≥ emitted holds. INERT epoch test FLIPPED (epoch now advances). Tests: commit-ok end-to-end (seal+swap+counter-advance) + fail-closed-no-swap (no-channel/transport/forged → 0x46, same cap still accepted = no swap proof) + unsealable-candidate-never-commits (over-size candidate → 0x46 with the installed channel's commit-call-counter == 0 = the seal-fail short-circuits BEFORE the durable commit). Compiles always, RUNS only under `agent-keygen-exec-preview` (no candidate otherwise). **6-4b (NEXT):** wire `install_commit_channel(Box::new(VsockBootRelayChannel::new(VMADDR_CID_HOST, relay_port)))` into `run_agent_gateway_boot` AFTER Ready, before serve (cfg gated). **6-4a REVIEW TIER = FULL MATRIX** (it carries the load-bearing INERT→LIVE epoch/structural flip + the seal-before-emit insertion; the lighter staged cadence is for 6-4b boot-wiring only). **INTERIM STATE (6-4a merged, 6-4b pending):** no commit channel is installed in a real boot, so `commit_candidate_to_anchor` always hits "no channel ⇒ SealFailed" — GENERATE_KEYS-exec returns 0x46 UNCONDITIONALLY even under `agent-keygen-exec-preview` until 6-4b lands; this fail-closed interim is BY DESIGN, not a regression. **NONCE — discharges ORIGINAL-6-4 OWNS(a):** 6-4a chose a THIRD option over the binary in the retained text below — a FRESH per-op `getrandom` draw (NOT serve-slot reuse, NOT a multi-slot source), side-stepping reuse entirely; the serial invariant is enforced STRUCTURALLY by holding the keystore mutex across seal→commit→swap (no separate in-code assertion to look for). 6-4a also discharged OWNS(c) (key-4 `request_id` plumbing); 6-4b owns OWNS(b) (boot install). **AVAILABILITY (§8 follow-up):** `COMMIT_ROUND_TRIP_BUDGET=10s` is a HARDCODED const (NOT threaded from the boot budget); the per-op commit holds the keystore lock across the vsock round-trip, blocking ALL agent ops (incl. read-only PUBLIC/PROVE) up to 10s — acceptable under the serial single-client model, flagged for §8. **EFFICIENCY (§8 follow-up, from /code-review):** the reorder computes the FULL `seal_body` (validate + two CBOR passes + AEAD) before the commit and discards it on any commit failure; the deterministic fail-closed-before-commit guarantee actually only needs `validate()` + the pass-1 size count (the `BlobTooLarge` gate), so a future shave could split `seal_body` into a cheap sealability precheck (before commit) + the full serialize/AEAD (after commit, before swap) — deferred (adds a precheck-vs-seal divergence risk to security-critical ordering code for a preview-only path; current order is correct + simple). ORIGINAL 6-4 desc (retained for the wire/NAK spec ONLY — BOTH the OWNS(a) nonce-concurrency framing AND the original `commit→seal` step order below are NON-NORMATIVE / SUPERSEDED; the as-built order is SEAL→COMMIT→swap→emit per the 6-4a note above): — [SUPERSEDED order was commit→ack→seal(new epoch)→swap→emit; as-built computes the seal FIRST then commits] under the keystore lock; any non-`Ok(())` from `verify_commit_ack_bytes` ⇒ fail closed, no emit. (There is NO explicit "NAK" frame: a refused/dropped commit surfaces as transport-error, timeout, or an ack that fails one of the 8 echo checks — all fold to the same fail-closed path. A negative-ack wire variant is a deferred extension, not this slice.) **OWNS (historical label definitions for the references in the 6-4a note above — (a)+(c) RESOLVED in 6-4a, (b) STILL PENDING in 6-4b; the original either/or framing is SUPERSEDED, do not implement it):** (a) per-op nonce generation — RESOLVED, **as-built = a FRESH per-op `getrandom` draw** (NOT the original "serve-slot-reuse-IFF-serial OR keyed/multi-slot" alternatives); (b) the boot-time commit-channel INSTALL — PENDING (→ 6-4b); (c) key-4 `request_id` plumbing to the frame layer — RESOLVED (in 6-4a). **ORDERING SAFETY (6-4 ships before 6-5 deliberately):** GENERATE_KEYS exec is behind the OFF-BY-DEFAULT, release-banned `agent-keygen-exec-preview` gate (NON-user-reachable in production until TASK-18 un-gates it), so 6-4 is never live for real custody before 6-5's idempotency proof lands; AND 6-4 already reuses the DETERMINISTIC request_id (so a retry of the same op is idempotent at the anchor) + the read-side AdoptForward crash-recovery is ALREADY merged (5b-2e) — 6-5 adds the request_id-dedup VERIFICATION + the structural-gap asymmetry TESTS, not a missing mechanism.
     **6-5:** `request_id`-keyed idempotency + crash-reconcile (counter/spend gap⇒AdoptForward; structural gap⇒StructuralGap⇒restore) tests vs the lab stub. **`request_id` CONTRACT (THREE distinct CBOR keys — do not conflate):** the op's `request_id` is the inner-envelope **key 4** (`agent_dispatch` decode, ≤ `MAX_REQUEST_ID_LEN`=64 bytes); the capability map carries its OWN copy at **key 10**, which `verify_capability` checks `== envelope.request_id` (key 4) AND binds into the `payload_binding` keccak preimage (`opcode ‖ [sub_op] ‖ request_id ‖ params`); the commit-request/ACK wire frame carries it at **key 8**. The commit reuses the op's envelope (key-4) `request_id` VERBATIM as wire key 8 — NOT a fresh enclave draw. It is host-supplied but capability-signature-bound (via the key-10 echo + payload_binding), so a retry of the SAME op carries the SAME `request_id` → the anchor's commit record is keyed by the `request_id` ALONE (the LOGICAL-op identity, commit-at-most-once) and a re-bump is idempotent (no double-advance); the anchor MUST treat a second commit under an already-recorded `request_id` with MATCHING `{epoch,structural,marks}` as the same op (return an ack — re-signed for the current fresh nonce), and the SAME `request_id` proposing ANY DIFFERENT `{epoch,structural,marks}` — **including a different epoch** (the cross-crash double-advance a post-AdoptForward re-issue of the same op would attempt; keying by `(request_id,epoch)` would WRONGLY admit it) — as a conflict ⇒ reject. (Plumbing the key-4 `request_id` from the capability/dispatch layer to the frame layer is owned by 6-4.) **VALIDATION DEPENDENCY (pin in the 6-5 idempotency proof):** the commit path uses `env.request_id` VERBATIM as the anchor idempotency key with NO non-empty/length check of its own — correctness of the `request_id` keying rests ENTIRELY on the upstream `MAX_REQUEST_ID_LEN`=64 decode bound (an empty or under-validated id would weaken the keying the anchor relies on); 6-5 MUST assert this dependency explicitly rather than leave it implicit. **6-5 AS-BUILT (DONE-pending-review):** no new load-bearing PRODUCTION code — the mechanism (AdoptForward, StructuralGap, request_id plumbing) was already merged (5b-2e + 6-4a/6-4b). Delivered: (a) **lab-stub idempotency** — the lab anchor (`lab_agent_smoke.rs`, release-banned) is now STATEFUL: a `LabCommitLedger` keyed by `request_id` (the LOGICAL-op identity, commit-at-most-once) → `(epoch, structural, marks)` durable record, threaded through `lab_anchor_pump_one`/`run_lab_anchor_stub`; a retry under the same request_id + same `{epoch,structural,marks}` re-signs an ack for the CURRENT (fresh) nonce (no double-advance — the nonce is NOT in the record), and ANY mismatch — different marks OR a **different epoch** (the cross-crash double-advance a post-AdoptForward re-issue would attempt; keying by `request_id` ALONE, not `(request_id,epoch)`, catches it) — is REJECTED (enclave then fails closed); deviceless conformance test `stub_commit_idempotent_per_request_id_and_rejects_conflict` (4 cases incl. the cross-epoch conflict). **NB the compact (job 7365) caught that an initial `(request_id, epoch)` keying WRONGLY admitted a different-epoch re-issue as a fresh entry — corrected to request_id-keying.** (b) **crash-reconcile** — the post-commit-crash decisions (sealed@N vs anchor@N+1: EpochOnly→`AdoptForward`, Structural→`StructuralGap`→restore) were ALREADY pinned by `reconcile_fresh_adopt_and_failclosed`; framed there explicitly as the slice-6-5 post-commit-crash proof. (c) **validation pin** — `envelope_request_id_decode_bound_pins_the_anchor_idempotency_key`: the envelope key-4 decode bound `MAX_REQUEST_ID_LEN`=64 rejects a 65-byte id `Malformed` (never reaches the commit), admits [0,64] incl EMPTY; empty-id is admin-signed-capability-bound (key-10 echo + payload_binding), NOT a host-substitute attack. (d) **3-key**: key-10==key-4 pinned by `generate_keys_request_id_mismatch_rejected`/`request_id_mismatch_rejected`; key-4→key-8 (commit-wire) is implicit in the success path (the ack's request_id echo would fail verify otherwise). All deviceless / lab-stub (CI-covered: default + lab-agent-smoke lanes); no SNP needed.
     **6-6:** reconcile's `structural_version` gate is ALREADY live — `agent_anchor::reconcile` compares structural and routes structural-ahead → StructuralGap → FailClosed/restore — so 6-6 is NOT enabling a dead gate (do not look for one). What 6-6 makes TRUSTWORTHY: the 6-2 atomic co-advance (within ONE committed history, structural_version never advances without freshness_epoch — NOT "any two states at epoch N share structural"; reconcile still compares structural EXPLICITLY) eliminates the pre-6-4 structural-only bump's FALSE StructuralGaps, so the already-live gate stops mis-firing; 6-6 = that invariant + the asymmetry tests that pin it, NOT new branch logic. **6-6 AS-BUILT (DONE-pending-review):** no new PRODUCTION code (test-only) — one deviceless test `co_advance_invariant_drives_reconcile_asymmetry` (agent_keystore.rs; asserts `marks == lmarks` before each reconcile, since reconcile ignores marks in the epoch-ahead arm — so a regression mutating a marks surface on an epoch-only bump can't pass) that drives the REAL `advance_commit_epoch` co-advance THEN `reconcile`, connecting cause→effect (the existing tests covered each in isolation): EpochOnly advance keeps structural ⇒ `AdoptForward` (+ a RUN of 5 epoch-only advances still AdoptForward — pins NO false StructuralGap can accumulate); structural advance moves structural ⇒ `StructuralGap`⇒restore (the asymmetry); same-epoch diverged-structural ⇒ `Inconsistent` (the invariant's contrapositive). `advance_commit_epoch` leaves marks unchanged (already pinned), so every anchor shares local's marks → isolates the structural asymmetry. The reconcile gate + advance_commit_epoch atomicity were already merged (5b-2e + 6-2); 6-6 just pins the join.
     **6-7:** shared `commit_before_emit` seam for the deferred handlers + lab-agent write-path SNP smoke. **Deferred-handler bump-class REFINEMENTS (from the 6-2 review — the opcode-level `commit_bump_class` is fail-closed-safe but coarse; resolve WITH each handler):** (a) the CONFIGURE_TREASURY handler MUST add a SUB-OP-level classifier — `set_limits`/`refill_budget`/`raise_lifetime_breaker` = Structural, but `reset_lifetime_breaker` is marks-only (spend resets + `strict_recovery_counter`) = EpochOnly (whole-opcode Structural OVER-classifies it → avoidable StructuralGap→restore service-denial); (b) the EXPORT_BACKUP handler MUST resolve `audit.last_exported_seq` anti-rollback coverage — it is NEITHER a marks surface NOR `structural_version`, so under the current EpochOnly an EXPORT whose seal drops adopt-forwards and silently rolls `last_exported_seq` back (audit-completeness/AC#14 weakening); make it marks-covered, prove rollback acceptable, or class EXPORT Structural. (Both are forward-declarations today — the handlers + the audit-ring write path are deferred/fail-closed.) Decisions: commit keyed by the op's envelope (key-4) `request_id`, carried on the commit/ACK wire frame at key 8 (idempotent retry — see the 6-5 three-key contract); ack echoes the full `{epoch,structural,marks}` tuple (defense-in-depth); per-op nonce on the commit/ACK frame at wire key 7; proceed against the lab stub like the read-side; commit-failure dispatch code = reuse 0x46 (decided in 6-4). lease=N (low-value faucets only) is a deferred extension, NOT this slice. **6-7 DECOMPOSITION (per the Understand 2026-06-14):** split into **6-7a** (the seam — buildable now) + **6-7b** (the lab write-path SNP smoke — needs aya; **ACCEPTANCE:** a `disk-production-lab-agent-gateway`-style image with `agent-keygen-exec-preview` ON + the boot commit-channel installed [6-4b] + the lab anchor stub running [6-5 stateful], drives a real signed GENERATE_KEYS over the 0x40 serve port and asserts the full seal→commit→swap→emit phase sequence [cap-verify → seal → 0x45 commit-request → ack-verify → swap → 0x40 reply carrying the key list + sealed blob]; PASS = ≥2 consecutive SNP runs on the Turin guest emitting a `RESULT PASS phases=N` marker, mirroring the 5b-2c-iii read-path smoke; **6-7b further SPLIT into 6-7b-i (deviceless, CI-coverable) + 6-7b-ii (the aya SNP runs)**. **6-7b SCOPING FINDINGS (Understand 2026-06-14):** (1) **NO hidden prerequisite** — the anti-rollback binding install is ALREADY wired in the smoke bin: `run_boot_anti_rollback_handshake → boot_reconcile_anti_rollback → install_anti_rollback_binding` on the Fresh arm (the "UNWIRED — 5b adds the only caller" doc comments in `agent_boot.rs`/`agent_boot_driver.rs` are STALE; 5b landed). So after a Fresh boot the AC#5 Layer-2b gate is OPEN and GENERATE_KEYS can execute under `agent-keygen-exec-preview`; the commit channel is likewise installed at boot under that cfg (6-4b, `agent_gateway_boot.rs` step G'). (2) **Real blocker fixed in 6-7b-i** — the smoke keystore's `admin_authority_pk` was a placeholder `[0xa1; 32]` (no signing key ⇒ no valid cap), so the fixture is regenerated with a KNOWN `SMOKE_ADMIN_SEED`. **6-7b-i AS-BUILT (DONE-pending-review):** (a) fixture: `SMOKE_ADMIN_SEED=[0x0a;32]` → `admin_authority_pk` is its verifying key; golden blob + `.json` sidecar regenerated (new `admin_seed_hex`/`admin_authority_pk_hex` field-coupled sidecar asserts); read-path smoke UNAFFECTED (PUBLIC_IDENTITY ignores admin key). (b) cap-helper widenings (the established 1-line pattern): `agent_capability::test_signed_capability` + `cap_to_map` → `cfg(any(test, feature="lab-agent-smoke"))`; `agent_dispatch::generate_keys_canonical_params` → `pub(crate)` — so the release-banned lab client mints a GENERATE_KEYS cap SINGLE-SOURCED with the enclave's `payload_binding`/verifier. (c) `run_agent_keygen_smoke_client` (preview-gated) — W1: a real signed GENERATE_KEYS(count=2) → asserts the success reply's key list AND **unseals the resealed blob** (under `SMOKE_SEAL_ROOT`+placeholder measurement) to a body with entries+2, structural_version+1, freshness_epoch+1 (Structural-op atomic bump) = the observable seal→commit→ack-verify→swap→emit witness; W2: a WRONG-key cap → `0x43` fail-closed (no seal/commit); terminal `RESULT PASS phases=2`. (d) `lab_anchor_commit_reply` refactored to a pure `lab_commit_ack_for_request` (decode→scope-guard→6-5 ledger→sign UNFRAMED ACK), shared by the stream stub AND the new deviceless `LabCommitChannel` (signs ACKs vs the smoke `anchor_root` via the SAME logic) so the in-process proof and the aya stub can't drift; cross-val harness `run_keygen_client_against_router` installs the keystore + anti-rollback binding + commit channel (+ resets the seal root to the cfg(test) reference = `SMOKE_SEAL_ROOT` so the client's unseal matches) and drives the client through the REAL `serve_framed_pump`; two tests — `keygen_client_drives_real_generate_keys_against_replica_router` (darwin) + `..._against_shipped_serve_glue` (linux+vsock+preview, the BINDING proof). (e) `twod-hsm-agent-keygen-smoke-client` bin (required-features `agent-gateway,vsock-transport,lab-agent-smoke,agent-keygen-exec-preview`) + a CI lane for the lab+preview combo (the shipped-glue cross-val runs deviceless in CI — the 6-4b CI-gap was a boot/SNP-pinned path, this one is deviceless ⇒ CI-coverable). Verified: darwin lib+bins+clippy-clean + linux cross-`check` of the gated test/bin + base(404)/preview(413 = 412 + 1 new 6-7b-i test)/write-path(17) lanes + read-path bins + regen-idempotent. **6-7b-ii (DONE-pending-review, aya-VALIDATED 2026-06-14 — branch task-7.7-6-7b-ii-keygen-snp-smoke @ 1f0e9cd):** AS-BUILT = (a) `enclave.nix` new `agent-gateway-keygen` profile (the SAME `twod-hsm-agent-gateway` bin + `agent-keygen-exec-preview`; separate profile keeps preview off the read-path image; debug build); (b) `flake.nix` `enclave-agent-gateway-keygen` package + `disk-production-lab-agent-keygen-smoke` output (reuses the 5b-2c-iii serve unit UNCHANGED — it already provisions `TWOD_HSM_PQ_SEAL_V1_ROOT_FILE` = the lab reference root = `SMOKE_SEAL_ROOT`, so the guest's production seal-root resolution matches the host client's unseal); (c) `run-nix-snp-agent-keygen-smoke.sh` (host drives `twod-hsm-agent-keygen-smoke-client`; asserts `RESULT PASS phases=2` PLUS a wire commit-witness = a NEW anchor-signed 0x45 ACK + relay pump AFTER boot, proving the per-op commit round-tripped on the wire not just in the client's in-band unseal); KVM-refusal arm reuses the `DISK_ATTR`-parameterized `run-kvm-agent-refusal.sh`; (d) CI eval-gate + SMOKE-PASS-CRITERIA entry. **LATENT BUG FIXED (a 6-7b-i regression the WHOLE 6-7b-i review missed — workflow + 4-cell matrix + /code-review + bots — because the disk image is EVAL-ONLY in CI, built only on aya):** `lab-prod-fixtures.nix` asserted the smoke keystore blob = 4418 bytes, but the 6-7b-i admin-key regen shrank it to 4416 ⇒ a real build of BOTH the read-path AND keygen images would have failed the size assert. Fixed 4418→4416; validated by the aya build getting PAST `twod-hsm-lab-agent-smoke-keystore.drv`. **ACCEPTANCE MET:** 2 consecutive SNP runs at 1f0e9cd both `RESULT PASS phases=2` (real GENERATE_KEYS seal→commit→ack-verify→swap→emit on the Turin guest: `generate-keys PASS` minted 2 keys + resealed blob unseals to entries+2/structural+1/epoch+1; `generate-keys-bad-cap PASS 0x43`; commit-witness OK each run) + KVM expected-refusal PASS (warn-outcome + err-render + 2 restart cycles, never served) + aya `lab-agent-smoke+agent-keygen-exec-preview` cargo suite 19/0 (incl. the linux shipped-glue keygen cross-val). **SLICE 6 (the write-side) IS NOW COMPLETE end-to-end on real SEV-SNP hardware.** Detail in SMOKE-PASS-CRITERIA.md §"Agent-gateway WRITE-path smoke". Original scope: the `disk-production-lab-agent-keygen-smoke` nix image (preview ON) + `run-nix-snp-agent-keygen-smoke.sh` runner + KVM-refusal arm + SMOKE-PASS-CRITERIA entry + ≥2 consecutive SNP `RESULT PASS phases=2` on the Turin guest. NB 6-7b-ii UNIQUELY exercises the PRODUCTION seal-root resolution at the SWAP (`set_pq_seal_v1_provisioning_root` from `TWOD_HSM_PQ_SEAL_V1_ROOT_FILE`), which 6-7b-i deliberately bypasses via the `cfg(test)` reference-root fallthrough (= `SMOKE_SEAL_ROOT`) — so the deviceless proof asserts the wire/ordering/ack-verify path while the SNP run additionally proves the production seal-root RESOLUTION mechanism, configured to the smoke root. **6-7b-ii ACCEPTANCE PREREQ (claude-design review):** the keygen smoke image MUST provision the production seal-root file (`TWOD_HSM_PQ_SEAL_V1_ROOT_FILE`) EQUAL to `SMOKE_SEAL_ROOT` (the `seal_v1_provisioning_root.bin` the lab fixtures already use) — or make the client's unseal root configurable — else the resealed-blob unseal assertion fails on the guest; the existing 5b-2c-iii agent unit already sets this var, so the keygen image inherits it. **6-7b-i REVIEW (5-dim adversarial workflow 2026-06-14):** 1 CONFIRMED LOW (the `lab-agent-smoke`-without-preview lab-bin lane dead-coded `cap_to_map`/`test_signed_capability` — both helper gates TIGHTENED to `any(test, all(lab-agent-smoke, agent-keygen-exec-preview))` to match their preview-gated consumer; verified the 2 warnings cleared + write-path 17 green); no correctness/fail-closed defects found, write-path proof confirmed end-to-end. **FULL GATE LEDGER:** Reduced Matrix codex-sec(job 7430)/gemini-sec(7432, ran clean)/claude-design(7431) + grok-sec via the native CLI cell = all No-issues/Pass; `/code-review` xhigh (9 angles + sweep); **compact (7437→7440) caught a MEDIUM the matrix+workflow MISSED** — W2's wrong-key phase used counter=1, already consumed by W1, so a sig-check regression would still 0x43 via the stale-counter path (auth-gate test vacuous) → FIXED counter=2 (the next-contiguous the counter check would ACCEPT, isolating the signature gate), compact 7440 re-verified "No issues"; sweep LOWs folded (assert strengthened: minted refs mutually distinct + ≠ seeded SMOKE_KEY_REF + seeded entry survives the swap; W1 marker → "entries+2"; stale `test_signed_capability` docstring; seal-root reset comment); PR bots — greptile 3 threads (P1 matrix-evidence replied w/ job IDs + resolved; 2×P2 entries+1op/`marks_round_trip`-Err FIXED + auto-resolved), gemini-bot "no feedback", coderabbit rate-limited; CI rust-test green. Squashes: d5ff95b→f00c729→ff23b27→e0fecf3 on PR #78) + **6-7c** (the bump-class refinements — BLOCKED: the CONFIGURE/EXPORT/RESTORE handlers are deferred `NotConfigured` stubs, so a sub-op classifier can't be WIRED without the handler that decodes the sub-op; AND the two parts differ in STAKES: (i) the CONFIGURE `reset_lifetime_breaker` over-classification is an OPTIMIZATION not a safety blocker — the coarse `commit_bump_class` is fail-closed-safe, so over-classifying it Structural at worst costs an avoidable StructuralGap→restore, NEVER a rollback. The EpochOnly refinement + its marks-coverage PROOF are a handler-slice obligation (the CONFIGURE handler is deferred — doesn't exist): reset advances `strict_recovery_counter` (marks key-4) and resets spend state, but the proof that it touches NO non-marks surface must be checked against the REAL handler — `circuit_breaker_threshold` is NOT a marks surface and `lifetime_spend` is never-reset, so the actual reset target + its full marks-coverage are NOT assertable until the handler lands; (ii) the EXPORT `last_exported_seq` coverage is a SAFETY gap (AC#14 audit-completeness — `last_exported_seq` is NEITHER a marks surface NOR `structural_version`, so an EXPORT under EpochOnly whose seal drops would silently roll it back), SAFE TODAY ONLY because the EXPORT handler is a fail-closed `NotConfigured` stub — it MUST be resolved (marks-cover it / prove rollback acceptable / class EXPORT Structural) BEFORE the EXPORT handler ships, an explicit un-gate prerequisite). **6-7c DEFERRED to the handler slices** (CONFIGURE sub-op classifier → TASK-15/CONFIGURE handler; EXPORT `last_exported_seq` → BLOCKING prereq of the EXPORT handler / TASK-18 un-gate, NOT a mere optimization); the `commit_bump_class` docstrings already flag both caveats. **6-7a AS-BUILT (DONE-pending-review):** extracted the inline GenerateKeys seal→commit→swap→emit ordering into a shared `commit_before_emit<F: FnOnce(&[u8])->Vec<u8>>(candidate, request_id, measurement, guard, encode_response)` seam in `agent_dispatch.rs` — the ONLY per-opcode part is the `encode_response` closure (called ONLY after the commit succeeds); GENERATE_KEYS now calls it with `|sealed| encode_generate_keys_response(&keys, sealed)`. Behavior-preserving (the GenerateKeys frame tests — frame_handler/commit_failure/unsealable — pass unchanged); the deferred handlers reuse the seam when they land (the altitude fix the 6-4a /code-review flagged). preview 412/default 404/no-feature 94, clippy clean.
7. **AC#5 funding gate — Layer-1 + Layer-2b BOTH LANDED (Layer-1 = TASK-16, branch task-16-ac5-layer1-nix-funding-gate).** The Nix build-time assertion now lands: `guest-profile.nix` adds `agentAntiRollbackMode ? "none"` + the DERIVED `agentAntiRollbackEnabled = (agentTransferFaucetSignerPackage != null)` (a funding profile can't bypass by leaving a flag falsy) + `antiRollbackResidualOptOut ? false`; `nixos-module.nix` asserts `!(productionMode && agentAntiRollbackEnabled && agentAntiRollbackMode == "none" && !antiRollbackResidualOptOut)` in the `lib.optionals isProd` list (mirrors the mainnet trust/seal gate); `flake.nix` `checks.agent-anti-rollback-gate` exercises BOTH polarities + the derivation at eval (verified on aya — both the AC#5 check + the mainnet regression check build clean), wired into CI. **Remaining for full AC#5:** ONLY the AC#10 opt-out's RUST verbatim-text + operator-sig verification (`sealed_optout_acknowledged`, still a `false` stub) — a format-bump sub-slice; the opt-out ESCAPE is therefore not yet honorable, which means the **default HARD BLOCK is fully in force at both layers** (the safe state). Per §5 the transfer-wallet fund-custody block is a *Layer-1* (build-time) control; with Layer-1 landed, a production funding profile (one installing a faucet/transfer signer — TASK-15) now FAILS THE BUILD without an anti-rollback mechanism. The mode is a BUILD-TIME measured param (no runtime env channel), satisfying the §1406 env-channel measured-boot obligation for the mode by construction. This slice is an intentionally **early, dormant** guard — the `ANTI_ROLLBACK_BINDING` is installed only by the (deferred) boot-wiring slice after a successful reconcile, so in production today it is **unset ⇒ every rollback-sensitive op is hard-blocked** (no end-to-end execution path until slices 5/6 land; ordering-wise 7 ships before 5/6 deliberately as the fail-closed default). **Layer-2b (Rust runtime block) LANDED (`agent_dispatch`)**: `AgentOpcode::is_rollback_sensitive` {1,5,6,7,8} (SIGN_TRANSFER excluded; reads allowed); a gate after opcode-decode/before privilege-routing rejects rollback-sensitive ops with NotConfigured(0x45) when the boot-resolved `ANTI_ROLLBACK_BINDING` process-global is absent (fail-closed default; `install_anti_rollback_binding` is the boot-wiring-only setter, called post-reconcile; CONFIGURE_TREASURY gated whole-opcode). 9 gate tests + 14 existing tests install a test binding (serialized via a test mutex). **DEFERRED:** the measured/sealed AC#10 opt-out (verbatim text + operator-sig in sealed config — a format-bump sub-slice; `sealed_optout_acknowledged` is a `false` stub so the gate hard-blocks meanwhile, the safe default); **Layer-1 Nix assertion** (guest-profile, TASK-16); **Layer-2a** `compile_error!` is forward-looking (no stub anti-rollback feature exists yet). Decision: reuse NotConfigured(0x45) (keeps §10.9 band + existing variant-equality tests; verbatim message in a code comment — AC#5 requires a fail-closed reject, not a distinct wire string). **Deferred-sub-slice owners (so AC#5 completion isn't missed at un-gate):** Layer-1 Nix assertion → **TASK-16**; AC#10 measured/sealed opt-out (format-bump) + Layer-2a release `compile_error!` → blocking prereqs of the live-GENERATE_KEYS un-gate **TASK-18** (alongside the existing 7.7/scope-target/audit prereqs). AC#5 is not "done" until Layer-1 lands.

Review evidence for THIS implementation slice (PR #45, AGENTS.md Full Matrix on `impl/` state-machine logic): `/code-review` xhigh (7 finder angles) + **roborev Full Matrix on the branch** — codex security+design, gemini security+design, claude-code security+design, grok security (opencode xai/grok-4.3, confirmed `done` not skipped) — then `roborev compact --wait` consolidated 14 jobs → job 7941 (2 residual: the documented strict-decode deferral + a stale-text fix, both handled). PR review bots (gemini-code-assist, CodeRabbit, greptile) replied. (This is distinct from the 524c8d8 matrix, which covered the design doc.) Fixes folded in: single-predicate signed-preimage count; `chain_block_hash` surfaced on `AnchorState`; malformed/invalid-point/chain-downgrade+upgrade negative tests; contract docstrings (nonce freshness, strict-decode, monotone-marks seeding); wire format relabelled v1-PROVISIONAL with structural_version/marks_payload pinning deferred to the blocking sub-slices above; active-active scope claim corrected (Option A is not clone-safe). **Follow-up cleanup: DONE in PR-A** — the duplicated CBOR map accessors (`map_get`/`as_u64`/`as_bytes`/`as_bytes32`/`as_digest`) across agent_capability/agent_dispatch/agent_anchor are now a single `agent_cbor` `pub(crate)` home (`as_bytes_n::<N>` unifies the 32/64-byte reads).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Design delivered in backlog/docs/agent-gateway-anti-rollback.md (PR #33, squash e5d3213). Production anti-rollback for sealed replay counters + faucet spend caps. Platform: SEV-SNP has NO per-enclave hardware monotonic counter -> external anchor required. Selected Option A = remote monotonic counter + epoch-lease: freshness_epoch in the pq-agent-keystore-v1 encrypted body (format extension, version bump per 7.2 AC#16); mutual-authenticated anchor handshake (agent-domain SNP report_data + Ed25519-signed anchor response vs pinned anchor_root); on epoch != anchor-current never trust the stale blob's own marks (anti-rollback): ADOPT the anchor's counter/spend marks when they fully resolve the gap (bounded crash-reconcile), else FAIL CLOSED (epoch > anchor-current anchor-rollback, structural key/config gap -> restore, or anchor unavailable); per-dispense bump+seal-before-emit; default lease=1, safe lease=N only via per-spend anchor-ack (count-bounded, never time); crash-reconcile keyed by request_id. Covers cap counters + faucet cumulative/lifetime spend + strict recovery counter (AC#2); boot/restore seed from authenticated marks never-zero (AC#3); active-active operator-procedural under A, enforced only by Option B global ledger (AC#4); AC#5 funding gate = 2-layer fail-closed (Nix assertion with explicit opt-out term + derived enabled, Rust block on rollback-sensitive commands with SIGN_TRANSFER excluded/EXPORT+RESTORE included) + hard-block-default + measured/sealed audited opt-out. Verified by roborev 3x3 + compact + the /code-review skill (40->15) + all 9 PR bot comments resolved/replied (CodeRabbit confirmed). The DESIGN above is complete; the **anti-rollback anchor implementation is owned by TASK-7.7 itself** (slices tracked in the Implementation Notes — slice 1 `agent_anchor.rs` verify+reconcile landed), while TASK-7.6 owns the Agent Gateway secp256k1 signer backend it binds onto.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Anti-rollback design or production-funding block is documented.
- [x] #2 Failure and rollback scenarios are covered by tests, vectors, or reviewed runbook validation where code does not yet exist.
- [x] #3 Final summary added before marking Done.
<!-- DOD:END -->
